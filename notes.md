targets = sprites?
always 1 more than the number of sprites

monitors = the little display thingies that tell you the value of a variable

blocks:

motion:
foo_menu is the option box in the thing foo is the block itself