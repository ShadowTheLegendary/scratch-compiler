[package]
name = "scratch-compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.11.1"
reqwest = {version = "0.13.4", features = ["blocking"]}
serde_json = "1.0.150"
strum = { version = "0.28", features = ["derive"] }
zip = "8.6.0"
